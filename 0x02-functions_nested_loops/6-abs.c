#include "main.h"
/**
 * _abs - computes the absolute value of un interger
 * @c: number to be computed
 * Return: Absolute number of the value or zero
 */
int_abs(int c)
{
	if( c < 0 )
	{int abs_val;
		abs_val = c * -1;
		return (abs_value);
	}
	return(c);
}
